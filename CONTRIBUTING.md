# Contributing

Issues and pull requests are welcome.

Before submitting a change, run:

```bash
python3 -m pip install -r requirements.txt
zsh scripts/build-app.sh
```

Do not add third-party cursor artwork to the repository unless you own it or its license explicitly permits redistribution.
